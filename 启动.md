# MuseTalk Docker Compose 启动指南

## 启动 Docker Compose

### 方式1: 前台启动（查看实时日志）
```bash
docker compose -f docker compose.dev.yml up
```

### 方式2: 后台启动
```bash
docker compose -f docker compose.dev.yml up -d
```

### 方式3: 重新构建并启动（如果修改了 Dockerfile）
```bash
 docker compose -f docker-compose.dev.yml up --build
```

## 常用命令

### 查看容器状态
```bash
docker compose -f docker compose.dev.yml ps
```

### 查看日志
```bash
docker compose -f docker compose.dev.yml logs -f
```

### 停止服务
```bash
docker compose -f docker compose.dev.yml stop
```

### 停止并删除容器
```bash
docker compose -f docker compose.dev.yml down
```

### 进入容器内部（调试用）
```bash
docker exec -it musetalk_dev bash
```

## 启动后验证

服务启动成功后，访问以下地址验证：

- **API 文档**: http://localhost:8083/docs
- **健康检查**: http://localhost:8083/health
- **就绪检查**: http://localhost:8083/readiness

## 注意事项

- 服务监听端口：8083
- 调试器端口：5678（需要 DEBUG=1 环境变量）
- 首次启动可能需要 40 秒等待模型加载
- 使用 Ctrl+C 停止前台运行的容器
